#include <iostream>
#include <vector>
#include <multiset>
#include <algorithm>
using namespace std;

vector<double> medianSlidingWindow(vector<int> &nums, int k)
{
     
}

int main()
{
    vector<int> res = {1, 3, -1, -3, 5, 3, 6, 7};
    for (auto x : medianSlidingWindow(res))
    {
        cout << x << " ";
    }
}