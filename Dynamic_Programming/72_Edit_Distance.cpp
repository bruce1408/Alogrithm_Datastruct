#include<iostream>
#include<vector>
using namespace std;

int minDistance(string w1, string w2) 
{
    int n = w1.size(), b = w`2.size();

    string a = ' ' + w1, b = ' ' + w2;
    int n = a.size(), b = w2.size();
    vector<vector<int>>f(n, vector<int>(m));

    int a = 
}