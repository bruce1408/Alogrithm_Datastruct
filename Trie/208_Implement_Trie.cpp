#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

/**
 * 306 累加数字
 * 判断这个序列是不是一个菲波那切数列
 * 如果是的话就返回1，否则返回0
*/

/**
 * 方法 1，使用高精度的方式来保存每个数字，计算相邻两个数的和
 * 是不是和下一段是不是匹配，如果整个都可以匹配完，那么就是可以返回1
*/
bool isAdditiveNumber(string num)
{

}
